传感:
index_history（History路由）：
1、formattedRows变量赋值为对应实际响应的值
2、toMap函数硬编码的map操作体内容修改

index_history（data路由）：
1、formattedResult调用push的数组entry的内容

test（test_history路由）:
1、toTwoArray方法中的formattedRows变量赋值硬编码内容
2、toMap方法中map具体操作数组的内容


行为:
index_action（action路由）：
1、toTwoArray方法中的formattedRows变量赋值硬编码内容
2、toMap方法中map具体操作数组的内容

index_action（data/action路由）：
1、search_result方法中的formattedResult变量的entry指定赋值


！！！传感指令发生修改则修改mqtt_server_get中的映射数组即可

上述路由内容伴随前端需要渲染内容的不同进行相应的维度修改,而前端维度修改注意事项如下：
DEVICE.vue页面：
tbody中的td进行铭牌相应内容的数组赋值

experience.vue页面：
无

History.vue页面：
1、传感数据的字段控制数组赋值以及单位控制数组的赋值
2、具体的分页数组元素赋值item
3、行为情况下的前端数据赋值
4、enough函数柱状图总量变量的赋值（含传感数据以及行为）

Home.vue页面：
无

Now.vue页面：
1、传感数据的字段控制数组赋值以及单位控制数组的赋值
2、最新传感数据元素赋值表格
3、最新行为数据元素赋值表格
4、y_zhi曲线图函数中的average数组元素个数初始化指定
5、recordDate变量指定索引位置的时间赋值
6、y_zhi1柱状图函数中的average数组元素个数初始化指定
7、recordDate变量指定索引位置的时间赋值
8、nowArray的时间变量格式化渲染(item[index]形式赋值的位置)
9、nowArray1同理
10、时间数组内容添加元素的操作形如    time_array.push(tem_array.value[1][i][8]);--包含行为维度的情况
11、柱状图数据清空操作：
chartOption1.value.series[index].data = [];

t_container.vue页面:
1、储物箱具体数据数组元素赋值表格

t_device.vue页面：
1、工位选择器中的item数组元素赋值
2、直接工位赋值位于:['工位1','工位2','工位3']
3、工位数量改变的情况下修改yong_calculator中的循环次数以及最优解的分支判断赋值

t_error_msg.vue页面：
1、错误具体数据元素赋值item[index]的修改
2、错误数据维度修改了的情况下修改result_array的赋值以及分支判断内容和操作内容

test.vue页面：
1、总量维度发生修改则修改相应的变量或添加删除变量的值
2、y_zhi函数的数组元素固定大小初始化赋值的指定，recordDate变量的时间值赋值指定record的下标的修改
3、三种维度的变量赋值的修改替换操作(温度湿度光照)
4、打印操作指定字段的值的修改
5、websocket操作变量内容的直接修改
6、修改维度下的参考data_data的指定下标位置的元素赋值

App.vue页面：
1、路由页面的修改以及指定index.ts文件的修改

下方为路由/文件的对应关系
/t_device==>Device==>t_device.vue>工位导航选择页面
/t_error_msg=>Error_message==>t_error_msg.vue>错误信息页面
/New==>New==>Now.vue>实时数据以及监控室页面
/History==>History==>History.vue>历史数据页面
/t_Control==>Mqtt==>Control.vue>指令控制台页面
/test==>test==>test.vue>测试开始页面
/DEVICE==>DEVICE>铭牌信息页面
/sensor_set==>sensor_set>传感设置页面
/ex==>ex>实验页面
/==>/Device>导航页面